{
  "name": "the-designer-mcp",
  "version": "2.0.0",
  "description": "MCP server — design system selection, theming, anti-slop quality gates, component 8-state generator, project pre-flight scan, brand references, color palettes",
  "author": "Azzar Budiyanto",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
